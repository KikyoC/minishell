=======
unlink
=======
#include <unistd.h>

int unlink(const char *pathname);

supprime un fichier designer par le path
il est possible de supprimer un fichier qui est deja ouvert, le fichier sera toujours existant tant
que le fichier n'est pas fermer

reussite : code = 0
echec : code = -1

