===========
getcwd
===========
#include <unistd.h>

char	*getcwd(char *buf, size_t size);

recupere dans le buffer la le repertoire courrant (pwd)
on recupere le fichier sur lequel on est
si la size < que le chemin courrant : renvoie NULL


